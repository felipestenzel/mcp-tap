# Release v0.6.8 (Cache Fallback + Configure Dry-Run)

- **Date**: 2026-02-25
- **Issue**: #107
- **Status**: `done`
- **Branch**: `fix/2026-02-25-release-v068`
- **Priority**: `high`

## Problem

Changes from PR #106 are merged in `main`, but no release has been cut yet. Users cannot install
these improvements from package registries.

## Context

- Last release is `v0.6.7`.
- PR #106 added:
  - search cache fallback metadata (`#104`)
  - `configure_server(dry_run=True)` preflight (`#105`)
- Release process requires version bump, validation, tag publish, and smoke.

## Root Cause

Release cadence depends on an explicit bump/tag workflow after merge.

## Solution

Completed on 2026-02-25:

1. Bumped versions to `0.6.8`:
   - `pyproject.toml`
   - `npm-wrapper/package.json`
   - `uv.lock` package entry
2. Opened and merged PR #108 (`fix/2026-02-25-release-v068`) with green CI.
3. Published GitHub release/tag `v0.6.8`.
4. Verified publish:
   - PyPI API reports `mcp-tap` version `0.6.8`.
   - npm reports `mcp-tap` version `0.6.8`.
5. Real smoke checks from published artifacts:
   - `uvx --from mcp-tap==0.6.8 python -c "import mcp_tap; print(mcp_tap.__version__)"` -> `0.6.8`
   - `uvx mcp-tap@0.6.8 --help` -> exit code `0`
   - `npx -y mcp-tap@0.6.8 --help` -> exit code `0`

## Files Changed

- `pyproject.toml` — version `0.6.8`.
- `npm-wrapper/package.json` — version `0.6.8`.
- `uv.lock` — lock metadata updated to `0.6.8`.
- `docs/issues/2026-02-25_107_release-v068.md` — release tracking and completion record.

## Verification

- [x] Baseline tests pass before changes: `uv run pytest tests/ -q`
- [x] Post-change tests pass: `uv run pytest tests/ -q`
- [x] Linter passes: `uv run ruff check src/ tests/`
- [x] Ruff format check passes: `uv run ruff format --check src/ tests/`
- [x] Tag `v0.6.8` published
- [x] Registry availability confirmed (PyPI and npm)
- [x] Smoke real executed from published version

## Lessons Learned

- `gh pr create` with backticks inside double-quoted shell strings can corrupt PR body due command
  substitution; use `--body-file` for deterministic PR descriptions.
